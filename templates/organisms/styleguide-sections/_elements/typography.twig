{%
  set headerStyles = [
  'h1',
  'h2',
  'h3',
  'h4',
  'h5',
  'h6',
]
%}

{%
  set textStyles = [
  'body-base',
  'body-sm',
  'link-sm',
  'body-xs',
  'tag',
  'utility-xl',
  'utility-lg',
  'utility-lg font-bold',
  'utility-sm'
]
%}
{#Pre-renders tailwind classes that havent been used yet#}
<span class="hidden body-base body-sm body-xs link-sm utility-xl utility-lg utility-sm"></span>

<h2 class="h3 mt-6 mb-sm">Typography</h2>
<p class="body-sm mt-zero mb-sm">Defined in tailwind-typography.json. Use classes in twig templates to apply styles
  .</p>
<p class="body-sm mt-zero">Use headings to improve scannability and provide context for users to better understand the page. Headings also help structure page content for screen readers and search engines.
  Headings should be logically structured on a page using proper hierarchy, headings are ranked "h1" through "h6". A
  page starts with the H1 heading, which describes a page's main topic— the title of the page. Subsections can be
  organized with "h2" level headings. Those subsections can themselves be divided with "h3" level headings, and so on.</p>
<hr class="mt-sm mb-sm">
<div class="ts-grid-col-2">
  <div>
    <h3 class="h5 mb-6">Header styles</h3>
    {% for style in headerStyles %}
      <p class="{{ style }} mb-6">.{{ style }} Headline</p>
    {% endfor %}
  </div>
  <div>
    <h3 class="h5 mb-6">Text styles</h3>
    {% for style in textStyles %}
      <p class="{{ style }} mb-2">
        .{{ style }}
      </p>
    {% endfor %}
  </div>
</div>
