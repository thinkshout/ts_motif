{# Section Title
 * A section at the beginning of a block to introduce it, with optional links.
 *
 * Fields:
 * - eyebrow
 * - header
 * - text
 * - primary_btn
 * - secondary_btn
 * - header_tag
 *
 * Style:
 * - center
 * - left
 * - side-by-side (default)
 *
 * Colorway is defined by the parent block, and shouldn't be set on this component.
#}

{% set description_max_width = "730px" %}

{% if header %}
  {% if style == "center" %}
    {% include '@ts_motif/molecules/section-title/display/variants/section-title--align-center.twig' %}
  {% elseif style == "left" %}
    {% include '@ts_motif/molecules/section-title/display/variants/section-title--align-left.twig' %}
  {% else %}
    {# Default is "row" #}
    {% include '@ts_motif/molecules/section-title/display/variants/section-title--side-by-side.twig' %}
  {% endif %}
{% endif %}
