{# Solid Social media button
 * A color pill button.
 * Variations styled below.
  DEFAULT STYLES SET IN: tailwind-btns.json.
 *
 * Icon only -- adds screen reader text
 * - icon_only (boolean)

  HOW TO USE:
  btn-social:
  {% include '@ts_motif/atoms/btns/btn-social.twig' with {
      link: {title: 'learn more', url: '/'}
      size: 'md',
    }
  %}
#}

{# Primary values #}
{% set link = link %}
{% set add_classes = add_classes %}
{% set link_svg_name = link_svg_name %}
{% set icon_only = icon_only ?? false %}
{% set rel = "rel='noopener'" %}

{# btn classes, colorway, svg name #}
{% set btn_class = "btn-social" %}
{# Final set of classes #}
{% set link_classes = add_classes ? btn_class ~ ' ' ~ add_classes : btn_class %}

{% extends '@ts_motif/atoms/btns/btn-base.twig' %}
