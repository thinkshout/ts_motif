{# Outline button
 * An outlined pill button.

  * Button OPTIONS (text and outline)
  *   - size: 'sm', 'md' or 'lg

  * ICON OPTION
  *   - link_svg_name: 'download' (pass in svg name)
  *   - svg_position: 'before' (default) or 'after'

  HOW TO USE:
  {% include '@ts_motif/btns/btn-outline.twig' with {
      link: {title: 'learn more', url: '/'},
      size: 'sm'
    }
  %}
#}

{# Primary values #}
{% set link = link %}

{# btn classes, colorway, svg name #}
{% set btn_class = 'btn-outline' %}
{% set link_svg_name = link_svg_name %}
{% set size = size ?: 'lg' %}

{# Adds icon classes if needed#}
{% if link_svg_name %}
  {% set link_svg_classes = ' inline-block ' %}
{% endif %}

{# Combines classes, adds them to the btn template #}
{% set btn_class = 'btn-outline btn--' ~ size %}
{# Final set of classes, including any passed in #}
{% set link_classes = add_classes ? btn_class ~ ' ' ~ add_classes : btn_class %}

{% extends '@ts_motif/atoms/btns/btn-base.twig' %}
